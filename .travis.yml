language: java
sudo: false
install: true

addons:
  sonarqube:
    organization: "sgrillon14" 


jdk:
- oraclejdk8

script:
- mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar -Dsonar.host.url=https://sonarqube.com -Dsonar.organization=sgrillon14 -Dsonar.login=$SONAR_TOKEN -Punit-tests

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.sonar/cache'
    
notifications:
  email: false
